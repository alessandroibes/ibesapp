# API

REST consistente, OpenAPI, ProblemDetails, paginação, filtros, idempotência quando necessário, validação no servidor.
Rotas e nomes de recursos de domínio em português.
Não expor entidades EF diretamente.
Concorrência otimista em registros sensíveis.
